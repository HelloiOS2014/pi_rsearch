# Ch14: Delivery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create the final chapter — how to deliver the same agent core to different users via multiple modes (CLI/TUI/RPC/SDK), hook system, production deployment. Plus graduation challenge.

**Architecture:** MDX chapter with SourceReader referencing Pi's delivery modes. Demo `demos/14-deliver/` with problem (terminal-only) vs solution (multi-mode + hooks).

**Spec:** `docs/superpowers/specs/2026-03-25-harness-engineering-design-v2.md` Sections 8-9

---

## Parallel Execution: MDX (Tasks 1-2) || Demo (Task 3), then Task 4 (polish+push)

---

## Task 1: MDX First Half — Pain + Theory + Industry + Pi Modes

**Files:** Create `src/content/chapters/14-delivery.mdx`

Read Pi source:
- `pi-mono/packages/coding-agent/src/modes/print-mode.ts` (~125 lines) — the 125-line CLI delivery
- `pi-mono/packages/coding-agent/src/modes/rpc/rpc-mode.ts` — RPC protocol design
- `pi-mono/packages/coding-agent/src/core/sdk.ts` — SDK factory

Create ~4 export consts from real source.

Sections:

**`## 只有我一个人能用这个 Agent`** (~25 lines blockquote)
CI needs it for code review. IDE wants to embed it. Team needs approval hooks. Want to share a session.

**`## 为什么会痛`** (~10 lines)
Outermost layer of onion model. "Agent 核心是无 I/O 观点的库。一个 subscribe 就是一种交付。"

**`## 业界怎么解决`** (~30 lines)
Transport spectrum (stdio/SSE/Streamable HTTP/WebSocket). SDK design (Claude Agent SDK: query() → async iterator → 5 message types). Hook system as "programmable control surface" (18 events × 4 types).

**`## Pi 的三种模式，一个 AgentSession`** (~40 lines)
**Theory anchor:** "一个 controller，多个 observer。"

Interactive Mode (4,570 lines): full TUI.
Print Mode (125 lines): `subscribe` + `prompt` + output. Use SourceReader. "125 行代码服务一个完整的 CLI。"
RPC Mode (646 lines): JSON stdin/stdout, Unicode-safe JSONL parser.
SDK (376 lines): createAgentSession() factory.

Extension UI degradation: stub returns, `hasUI` check.

End with `---`.

- [ ] Build + commit

---

## Task 2: MDX Second Half — Hooks + Multi-Agent + Production + Graduation + Checklist

Append:

**`## Hook 系统：交付层的可编程控制面`** (~25 lines)
Hooks = how harness exposes control to consumers (CI, admins, security teams).
Claude Code: 18 events × 4 handler types. Decision model (allow/deny/modify/inject/halt).
Production use cases: security, quality, audit, context injection, cost control.

**`## 展望：多 Agent 编排`** (~15 lines)
NOT in demo scope. Three patterns: sub-agent (isolation), handoff (ownership transfer), parallel worktree (git isolation). Open problems: port conflicts, DB race conditions.

**`## 生产部署`** (~15 lines)
Three deployment models: ephemeral, long-running, hybrid. Session resumption (session_id + resume). State management options.

**`## 测试你的 Harness`** (~20 lines)
4 strategies table: Mock LLM, snapshot testing, property testing, integration testing. "测试 Harness 逻辑，不测试模型智能。"

**`## 不同产品为什么做了不同选择`** — comparison table
**`## 动手实现`** — point to demos
**`## 元技能`** — symptoms, evaluation
**`## 简化思考`** — standard protocols may replace custom RPC
**`## 检验标准`** — 5 items

**`## 毕业挑战`** (~20 lines)
Build a harness for a NON-coding agent (research assistant / data analyst / customer support). Must make design decisions about tools, trust, memory, config, delivery. Evaluation: can trace decisions to theory.

**Closing paragraph** — recap the entire Part 2 journey. "从 Ch9 的'Agent 失控了'到 Ch14 的完整交付，你已经走完了 Harness Engineering 的全部旅程。"

- [ ] Build + commit

---

## Task 3: Demo Scaffold + Both Files

Create `demos/14-deliver/`:

**01-terminal-only.ts** (~50 lines): Agent only works interactively. Can't be scripted, can't be embedded, no hooks.

**02-multi-mode.ts** (~180 lines): Implements:
- A `DeliveryLayer` class with `subscribe()` pattern
- `printMode()`: pipe prompt in, get result out (~20 lines)
- `rpcMode()`: JSON stdin/stdout protocol (~40 lines)
- `hookSystem()`: PreToolUse check with allow/deny (~30 lines)
- Show all three modes using the SAME agent core

- [ ] Install, run, typecheck, commit

---

## Task 4: Polish + Push

- [ ] Review MDX + demos, verify build, push to origin main
